# Cross Product Reference

このフォルダは、`コトメガネ` 単体ではなく、TECHIE 全体や複数製品横断で見る補助資料をまとめる場所です。

- `docs/` 直下の `kotomegane` 本体正本より優先しない
- Azure移行、共通基盤、UI統一などの横断テーマを置く
- `コトメガネ` 単体の current decision は `docs/` 直下の文書を優先する
