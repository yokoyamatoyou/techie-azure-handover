[pytest]
testpaths =
    tests
    note/tests
markers =
    legacy_compat: retired compatibility or stale frozen-expectation coverage kept outside default runs
    nicegui_main_file(path): specify the NiceGUI main file for browser simulation tests
