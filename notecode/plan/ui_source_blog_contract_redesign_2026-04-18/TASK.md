# ui_source_blog_contract_redesign_2026-04-18 TASK

この package は `naturalness_recovery_2026-04-07` を parked / not fixed のまま維持し、`opening_frame_redesign_2026-04-18` を reference line として保持したうえで、`UI article contract / source role separation / self-blog reuse policy` を docs-only top-level line として固定する。  
production code / tests / AGENTS / WORKLOG / inherited parked package docs は触らず、deadcode archive は inventory only に留める。

## Global Rules

- `naturalness_recovery_2026-04-07` は parked / not fixed のまま維持する
- `opening_frame_redesign_2026-04-18` は reference line として扱い、implementation continuation にしない
- current runtime mainline は keep し、zero-base redesign は docs-first artifact boundary として進める
- production code / tests / AGENTS / WORKLOG / inherited parked package docs は編集しない
- first line は docs-only とし、code diff は current user-explicit narrow slices で `1 owner / 1 slice` に落ちたときだけ始める
- actual archive は行わない
- archive work は candidate inventory だけに留める
- research と opening-frame notes は evidence として読むが source-of-truth に昇格させない
- package theme は `ui article contract / source role separation / opener boundary / self-blog reuse policy / minimal control axes` に固定する
- package-wide owner table は `not fixed` と書く
- first code step は current Phase 05 以外では `not started` と書く
- first implementation prompt を作らない
- `prompt_builder.py` simplification-first wording line の unchanged retry はしない
- `pipeline.py` current-first source ordering / hint triage の unchanged retry はしない
- `pipeline.py` core_message current-first hint の unchanged retry はしない
- `SECTION_SHADOW` reopen first に戻さない
- `quality_guard.py` first にしない
- repair acceptance reopen first にしない
- `natural_blog_core.py` first section history clamp 仮説は do-not-retry として継承する
- `output_formatter.py` formatter-only surface polish 仮説は do-not-retry として継承する
- `input_contract.py` upstream distilled summary 単独仮説は do-not-retry として継承する
- prompt accretion continuation 禁止
- fixed routing table 禁止
- planning / skeleton default reopen 禁止
- giant rewrite 禁止
- hidden reviser accumulation 禁止
- self-blog reuse を default fact source として扱わない
- `theme` を fact source として扱わない
- opener owner に `continuity` / `style_memory` を直接読ませない
- actual archive をしたくなったら停止して user report する

## Current Locked Outcome

- package category:
  - `DOCS_FIRST_UI_SOURCE_BLOG_CONTRACT_REDESIGN_REBUILD`
- current decision:
  - `keep current mainline, continue zero-base redesign line`
- current package state:
  - `naturalness_recovery_2026-04-07` は parked / not fixed
- opening-frame state:
  - `opening_frame_redesign_2026-04-18` は reference line
  - current owner prompt ではない
- initial docs outputs:
  - package docs 5 本
  - archive candidate inventory note 1 本
  - docs-only follow-up prompt 1 本
- actual archive:
  - `not started`
  - inventory only
- current narrow code owner:
  - `C:\tetie\notecode\note\simple_note_pipeline\prompt_builder.py` current Phase 05 slice only
- current narrow code step:
  - `completed closeout: prompt_builder.py generation prompt の writer-facing [SURFACE_REALIZATION_CARD]` handoff-only baseline + `note/tests/test_simple_note_pipeline.py` 1-file owner-local test lock
- package-wide owner table:
  - `not fixed`
- post-docs management verdict:
  - prior resting verdict は `stop_and_user_report`
- explicit user exception:
  - Phase 04 `Stage B artifact boundary minimum` の docs-only slice は許可する
  - Phase 05 `surface realization card first construction slice` の `prompt_builder.py` handoff-only slice は許可する
  - Phase 06 `surface realization card offline eval-first slice` の offline eval-first slice は許可する
- Phase 01 fixed baseline:
  - contract axes は `reader_task / evidence_mode / voice_distance / opener_mode / reuse_level`
  - `reuse_level` default は `none`
  - source role は `fact / continuity / style_memory`
  - `opening_frame` は `opener_mode` の subproblem として扱う
  - `紹介` / company introduction は `current_business_first`、history は support only
  - current runtime mainline は keep する
  - zero-base redesign は `contract / source / opener / reuse` artifact boundary として進める
- Phase 04 fixed boundary:
  - `fact packet` は fact-primary shadow artifact
  - `opener card` は opener-anchor shadow artifact
  - `continuity capsule` は support-only continuity summary
  - `style capsule` は content-free style summary
- next action:
  - Stage A telemetry-only scan は `pass / pass / pass (inferred) / pass (inferred)` まで閉じた baseline として維持する
  - explicit user exception で定義した Phase 04 `Stage B artifact boundary minimum` を current baseline として維持する
- completed Phase 05 closeout は completed baseline として keep し、current Phase 06 first slice artifact は `C:\tetie\notecode\logs\current_mainline_ui_runs\phase06_surface_realization_card_eval\` に固定する
  - Phase 06 first slice self-test artifact `summary.json / matrix.json / genre_issue_ledger.json / manual_review.md / research_notes.md` は生成確認済み
  - current summary read は `15 cases / short_gate 11/15 / announcement 2/5 / daily_story 5/5 / branding 4/5`
  - representative case json read は `execution_mode = offline_deterministic` / `compatibility_rebuild_applied = true` / `contract_alignment.compatibility_source = native_minimal`
  - `互換経路の動作確認用に最小本文を返します。` と `〜を具体化します。` の active root は `C:\tetie\notecode\note\newalgorithm_pipeline\pipeline.py` なので、eval result は `prompt_builder.py` 継続に落ちず `stop_and_user_report` を current boundary として維持する
  - grounding pass / semantic pass / source-opener-reuse boundary keep 後も fingerprint-side quality block が残り、current evidence だけでは next legal advance を決め切れないときだけ、future boundary note は single-question deepresearch trigger 1 問に限定する
  - user が deepresearch-first lane を explicit に選ぶときだけ、current legal move は package reopen ではなく `comparison note -> single-question deepresearch -> adoption note` の research/evidence lane に限定する
  - deepresearch-first lane の closeout は closed research/evidence lane として扱い、`surface realization card` は completed Phase 05 artifact、`surface profile` は alias only に留める
- completed Phase 05 は `C:\tetie\notecode\note\simple_note_pipeline\prompt_builder.py` の generation prompt handoff-only baseline と `C:\tetie\notecode\note\tests\test_simple_note_pipeline.py` 1-file owner-local test lock に閉じる
  - package-wide owner table / implementation prompt / actual archive は unresolved のまま維持する
  - 本文後半の `ready_for_stage_a_scan` 参照は management contract / historical route record であり、current active verdict ではない
- inherited do-not-retry:
  - `prompt_builder.py` simplification-first wording line
  - `pipeline.py` current-first source ordering / hint triage
  - `pipeline.py` core_message current-first hint
  - `SECTION_SHADOW` reopen first
  - `quality_guard.py` first
  - repair acceptance reopen first
  - `natural_blog_core.py` first section history clamp
  - `output_formatter.py` formatter-only surface polish
  - `input_contract.py` upstream distilled summary only
  - prompt accretion continuation
  - fixed routing table
  - planning / skeleton default reopen
  - giant rewrite
- package close state:
  - not closed
  - next action は Phase 06 artifact sync complete + `stop_and_user_report`

## Gates

### Entry Gate

- new package が `naturalness_recovery_2026-04-07` と role conflict していない
- new package が `opening_frame_redesign_2026-04-18` を reopen ではなく reference として扱っている
- exact code owner が `not fixed` のまま維持されている
- actual archive を前提にしていない
- research を evidence としてだけ扱っている

### Pass Gate

- docs 5 本が package category / objective / archive policy / next step で整合している
- docs 5 本と follow-up prompt が `reader_task / evidence_mode / voice_distance / opener_mode / reuse_level` を current contract axes として一致させている
- docs 5 本と follow-up prompt が source role を `fact / continuity / style_memory` で一致させている
- docs 5 本と follow-up prompt が `opening_frame = opener_mode subproblem` を維持している
- docs 5 本と follow-up prompt が `reuse_level default = none until Phase 02` を維持している
- docs 5 本と follow-up prompt が `reuse_level` を keep axis / gated runtime axis として一致させている
- docs 5 本と follow-up prompt が `continuity/style_memory = capsule only` と `opener owner does not read them` を一致させている
- docs 5 本と follow-up prompt が post-docs separate management step を `hold_docs_only / ready_for_stage_a_scan / stop_and_user_report` で一致させている
- docs 5 本と follow-up prompt が first candidate を `Stage A telemetry only` の artifact-level readiness として扱い、exact code owner を固定していない
- docs 5 本と follow-up prompt が `Stage A telemetry only = article_contract / fact sufficiency / opener support-only / reuse gate reason` を一致させている
- docs 5 本と follow-up prompt が `reuse_level = none` / fact insufficiency stop を legal Stage A output として維持している
- docs 5 本が Phase 04 の `fact packet / opener card / continuity capsule / style capsule` を同じ role boundary で定義している
- docs 5 本が completed Phase 05 closeout 後の stop boundary を `pipeline.py / quality guard / repair / routing / broader eval lane / threshold canonicalization / package-wide owner table / implementation prompt / actual archive still unresolved` で一致させている
- docs 5 本が current Phase 05 を `prompt_builder.py` 単独 owner / generation prompt handoff only として一致させている
- docs 5 本が `surface realization card` の minimum fields を `paragraph_breath / sentence_length_band / ending_mix / nominalization_budget / subject_visibility / connective_tolerance` で一致させている
- docs 5 本が current Phase 05 を opener direct-read / package reopen / broad redesign に読み替えていない
- docs 5 本が current Phase 05 closeout を owner-local verification pass + `stop_and_user_report` restored で一致させている
- docs 5 本が current Phase 06 first slice を `run_current_mainline_genre_sweep.py` の `live=False` limited eval + artifact review に一致させている
- docs 5 本が current Phase 06 target genres を `announcement / daily_story / branding` に一致させている
- docs 5 本が current Phase 06 self-test / retry rule を `artifact generation pass -> docs sync -> next slice`, `same slice retry up to 3 -> stop_and_user_report` で一致させている
- docs 5 本と helper docs / inventory note / follow-up prompt が、grounding pass / semantic pass / source-opener-reuse boundary keep 後も fingerprint-side quality block が残るときの future boundary note を single-question deepresearch trigger 1 問に限定している
- docs 5 本と helper docs / inventory note / follow-up prompt が、deepresearch-first lane の `comparison note -> single-question deepresearch -> adoption note` を closed research/evidence lane として扱い、current active code route に読み替えていない
- docs 5 本と helper docs / inventory note / follow-up prompt が、`surface realization card` を completed Phase 05 artifact、`surface profile` を alias only として一致させている
- companion inventory note が `archive-safe-now / archive-later-after-confirmation / do-not-archive` を区別している
- companion inventory note が `current_codex_autonomous_bootstrap.md` / `current_codex_state.md` / `current_codex_workflow.md` を `do-not-archive` に含めている
- follow-up prompt が new package docs を canonical prompt として参照している
- production code / tests / AGENTS / WORKLOG / inherited parked package docs を更新していない
- actual archive に進んでいない

### Stop Gate

- current package reopen と実質同じ内容しか書けない
- package objective が broad rewrite に膨らんだ
- exact code owner を early fix したくなった
- exact file mapping を early fix したくなった
- actual archive を今やりたくなった
- multiple worker 向け implementation prompt を作りたくなった
- self-blog reuse を fact source default にしたくなった
- `theme` を fact source に上げたくなった
- opener owner に `continuity` / `style_memory` を流し込みたくなった
- `Stage C / D / E` を first candidate にしたくなった

## Shared Checks

- `README.md` / `TASK.md` / `PROGRESS.md` / `ROLLBACK.md` / `EXECUTION_PROMPT.md` が `DOCS_FIRST_UI_SOURCE_BLOG_CONTRACT_REDESIGN_REBUILD` で一致している
- `README.md` / `TASK.md` / `PROGRESS.md` / `EXECUTION_PROMPT.md` が `first code owner = not fixed` を維持している
- `README.md` / `TASK.md` / `ROLLBACK.md` / `EXECUTION_PROMPT.md` が `actual archive = inventory only` を維持している
- `README.md` / `TASK.md` / `PROGRESS.md` / `ROLLBACK.md` / `EXECUTION_PROMPT.md` が contract axes を `reader_task / evidence_mode / voice_distance / opener_mode / reuse_level` で一致させている
- `README.md` / `TASK.md` / `PROGRESS.md` / `ROLLBACK.md` / `EXECUTION_PROMPT.md` が source role を `fact / continuity / style_memory` で一致させている
- `README.md` / `TASK.md` / `PROGRESS.md` / `ROLLBACK.md` / `EXECUTION_PROMPT.md` が `opening_frame = opener_mode subproblem` を維持している
- `README.md` / `TASK.md` / `PROGRESS.md` / `ROLLBACK.md` / `EXECUTION_PROMPT.md` が `reuse_level default = none until Phase 02` を維持している
- `README.md` / `TASK.md` / `PROGRESS.md` / `ROLLBACK.md` / `EXECUTION_PROMPT.md` が post-docs separate management step を `hold_docs_only / ready_for_stage_a_scan / stop_and_user_report` で一致させている
- `README.md` / `TASK.md` / `PROGRESS.md` / `ROLLBACK.md` / `EXECUTION_PROMPT.md` が first candidate を `Stage A telemetry only` の artifact-level readiness として扱い、exact code owner を固定していない
- `README.md` / `TASK.md` / `PROGRESS.md` / `ROLLBACK.md` / `EXECUTION_PROMPT.md` が `Stage A telemetry only = article_contract / fact sufficiency / opener support-only / reuse gate reason` を一致させている
- `README.md` / `TASK.md` / `PROGRESS.md` / `ROLLBACK.md` / `EXECUTION_PROMPT.md` が `reuse_level = none` / fact insufficiency stop を legal Stage A output として維持している
- `README.md` / `TASK.md` / `PROGRESS.md` / `ROLLBACK.md` / `EXECUTION_PROMPT.md` が Phase 04 の `fact packet / opener card / continuity capsule / style capsule` を同じ role boundary で定義している
- `README.md` / `TASK.md` / `PROGRESS.md` / `ROLLBACK.md` / `EXECUTION_PROMPT.md` が completed Phase 05 closeout 後の stop boundary を `pipeline.py / quality guard / repair / routing / broader eval lane / threshold canonicalization / package-wide owner table / implementation prompt / actual archive` unresolved で一致させている
- `README.md` / `TASK.md` / `PROGRESS.md` / `ROLLBACK.md` / `EXECUTION_PROMPT.md` が、grounding pass / semantic pass / source-opener-reuse boundary keep 後も fingerprint-side quality block が残るときの future boundary note を single-question deepresearch trigger 1 問に限定している
- `README.md` / `TASK.md` / `PROGRESS.md` / `ROLLBACK.md` / `EXECUTION_PROMPT.md` が、deepresearch-first lane の closeout を closed research/evidence lane として扱い、package reopen / code continuation に読み替えていない
- `README.md` / `TASK.md` / `PROGRESS.md` / `ROLLBACK.md` / `EXECUTION_PROMPT.md` が、`surface realization card` を completed Phase 05 artifact、`surface profile` を alias only として一致させている
- `README.md` / `TASK.md` / `PROGRESS.md` / `ROLLBACK.md` / `EXECUTION_PROMPT.md` が、current Phase 05 の exact owner を `prompt_builder.py` current slice only として一致させている
- `README.md` / `TASK.md` / `PROGRESS.md` / `ROLLBACK.md` / `EXECUTION_PROMPT.md` が、current Phase 06 first slice を `run_current_mainline_genre_sweep.py` の `live=False` limited eval に一致させている
- companion inventory note が `do-not-archive` に current runtime / current package / current prompt を含めている
- companion inventory note が current startup path docs を `do-not-archive` に含めている
- forbidden update がない

## Phase Map

### Phase 00 Top-Level Redesign Freeze

- Objective:
  - new top-level redesign line の objective / non-goals / archive policy / inherited boundaries を docs-only で固定する
- Hypothesis:
  - `UI article contract / source role separation / self-blog reuse policy` を opening-frame より上位の line として切り出せば、rename retry へ戻らずに next docs slice を持てる
- Owner:
  - `C:\tetie\notecode\plan\ui_source_blog_contract_redesign_2026-04-18\`
- Tasks:
  - package docs 5 本を作成する
  - archive candidate inventory note を作成する
  - docs-only follow-up prompt を作成する
- Exit:
  - objective / non-goals / archive policy / inherited boundaries が 5 本で固定される

### Phase 01 UI Article Contract / Source Role Baseline

- Objective:
  - current UI article types と source modes を minimal contract / minimal source role へ圧縮して固定する
- Hypothesis:
  - `reader_task / evidence_mode / voice_distance / opener_mode / reuse_level` で UI article types を説明し、source を `fact / continuity / style_memory` に分離すれば、カテゴリ別 patch accumulation なしに current runtime の contract 境界を説明できる
- Owner:
  - `C:\tetie\notecode\plan\ui_source_blog_contract_redesign_2026-04-18\`
- Tasks:
  - UI article type contract を few-axis で書く
  - source role selection / fallback rule を branch table ではなく slot responsibility で書く
  - `opening_frame` を `opener_mode` の subproblem として位置づけ直す
- Exit:
  - UI article type contract table と source role model が docs-only で説明できる
- Close note:
  - category 差は `reader_task / evidence_mode / voice_distance / opener_mode` を主軸に読む
  - `reuse_level` は phase close 時点では default `none` に留める
  - `opening_frame` は `opener_mode` の subproblem として package judgment に吸収する

### Phase 02 Self-Blog Reuse Policy / Minimal Control Axes

- Objective:
  - fixed 済み contract axes を保ったまま、self-blog reuse を `continuity reuse` と `style memory reuse` に分離し、current mainline を維持した zero-base artifact boundary として activation policy を固定する
- Hypothesis:
  - `reuse_level` を 5th axis のまま keep しつつ runtime gated axis として読み、`continuity/style_memory` を capsule only に固定し、opener owner を fact-only read に閉じれば、future implementation owner を early fix せずに zero-base redesign を narrow に保てる
- Owner:
  - `C:\tetie\notecode\plan\ui_source_blog_contract_redesign_2026-04-18\`
- Tasks:
  - `reuse_level` を keep axis / gated runtime axis として書く
  - continuity reuse の activation / retrieval key / fallback refusal を `continuity capsule` policy として書く
  - style memory reuse の activation / retrieval key / duplication guard / fallback refusal を `style capsule` policy として書く
  - `theme is not fact source` と `opener owner does not read continuity/style_memory` を明文化する
  - future zero-base stage order を artifact boundary で書き、file owner は固定しない
- Exit:
  - self-blog reuse policy が prompt accretion ではなく contract policy として説明できる
  - continuity / style が fact contamination を起こさず、raw past blog retrieval を canonical path に戻していない
  - opener owner が fact-primary / support-only だけを読み、continuity / style を直接読まないと説明できる
  - current mainline keep と future implementation owner unresolved を同時に維持できる

### Phase 03 Archive Candidate Confirmation

- Objective:
  - actual archive をせずに candidate inventory と do-not-archive boundary を確認する
- Hypothesis:
  - archive work を inventory only で止めれば、current mainline 参照を壊さずに cleanup の future boundary を作れる
- Owner:
  - `C:\tetie\notecode\docs\ui_source_blog_contract_archive_candidate_inventory_2026-04-18.md`
- Tasks:
  - archive-safe-now / archive-later-after-confirmation / do-not-archive を区別する
  - current runtime / current prompt / current packages を do-not-archive に固定する
- Exit:
  - actual archive をしていないことと candidate inventory だけが fixed されていることを説明できる
- Close note:
  - current startup path docs / current package / current prompt / current runtime boundary は `do-not-archive` の current baseline として保守する

## Post-Docs Separate Management Step

- Objective:
  - docs phases pass 後の management judgment を file owner fix / implementation prompt / code diff から分離し、next slice を docs-only で narrow に保つ
- Hypothesis:
  - `hold_docs_only / ready_for_stage_a_scan / stop_and_user_report` の 3 出力に絞り、first candidate を `Stage A telemetry only` の artifact-level readiness に限定すれば、locked decisions を崩さずに separate management を運用できる
- Owner:
  - `C:\tetie\notecode\plan\ui_source_blog_contract_redesign_2026-04-18\`
  - `C:\tetie\notecode\docs\current_codex_state.md`
  - `C:\tetie\notecode\docs\current_codex_work_instruction_prompt.md`
  - `C:\tetie\notecode\docs\separate_window_execution_prompt_ui_source_blog_contract_redesign_docs_followup_2026-04-18.md`
- Tasks:
  - management step の allowed outputs を `hold_docs_only / ready_for_stage_a_scan / stop_and_user_report` に固定する
  - first candidate を `Stage A telemetry only` に限定し、exact code owner / exact file mapping / implementation prompt / actual archive を未固定のまま保つ
  - `Stage A telemetry only` の read を `article_contract / fact sufficiency / opener support-only / reuse gate reason` に限定する
  - `ready_for_stage_a_scan` は implementation start ではないと明記する
  - owner fix / exact file mapping fix / implementation prompt / actual archive / `Stage C-D-E first` を stop boundary に置く
- Exit:
  - package docs / state / prompt helper が same management outputs と stop boundary を共有している
- Close note:
  - current phase は Phase 03 completed のまま維持してよい
  - separate management step は post-docs judgment であり、新しい code phase ではない
- current verdict は `stop_and_user_report` で、completed Phase 05 closeout 後の next forward move が `pipeline.py` / quality guard / repair / routing / broader eval lane / threshold canonicalization / package-wide owner table / implementation prompt / actual archive を要する boundary を返す
  - explicit exception route は Phase 04 docs-only slice と Phase 05 `prompt_builder.py` handoff-only slice までで閉じ、further docs-only slice は default で追加しない

## Stage A Readiness Scan Contract

- Objective:
  - `ready_for_stage_a_scan` を keep したまま、first candidate を telemetry artifact checklist の説明可能性に閉じる
- Hypothesis:
  - `article_contract / fact sufficiency / opener support-only / reuse gate reason` の 4 telemetry を current baseline として固定すれば、exact code owner / exact file mapping / implementation prompt / actual archive を unresolved のまま維持できる
- Owner:
  - `C:\tetie\notecode\plan\ui_source_blog_contract_redesign_2026-04-18\`
  - `C:\tetie\notecode\docs\current_codex_state.md`
  - `C:\tetie\notecode\docs\current_codex_work_instruction_prompt.md`
  - `C:\tetie\notecode\docs\separate_window_execution_prompt_ui_source_blog_contract_redesign_docs_followup_2026-04-18.md`
- Tasks:
  - `Stage A telemetry only` を 4 telemetry 項目の checklist として docs に固定する
  - `reuse_level = none` と fact insufficiency stop を legal Stage A output として維持する
  - Stage B の `fact packet / opener card / capsules` と exact code owner / exact file mapping / implementation prompt / actual archive fix を Stage A の current slice に混ぜない
- Exit:
  - package docs / state / prompt helper docs が same Stage A checklist と same stop boundary を共有している
- Close note:
  - Stage A は implementation start ではない
  - Stage B shadow artifact は Stage A の current first candidate ではない
  - explicit user instruction があるときだけ、Stage A close 後に Phase 04 として minimal boundary 定義へ進める

### Phase 04 Stage B Artifact Boundary Minimum

- Objective:
  - prior `stop_and_user_report` 後でも implementation lane を reopen せず、`fact packet / opener card / continuity capsule / style capsule` の role と stop boundary だけを docs-only で固定する
- Hypothesis:
  - Stage B を concrete file owner や prompt 設計ではなく shadow artifact の role boundary に限定すれば、locked decisions を崩さずに legal な post-stop docs-only slice を 1 本だけ持てる
- Owner:
  - `C:\tetie\notecode\plan\ui_source_blog_contract_redesign_2026-04-18\`
  - `C:\tetie\notecode\docs\current_codex_state.md`
- Tasks:
  - `fact packet` を fact-primary packet として定義する
  - `opener card` を opening-surface anchor artifact として定義する
  - `continuity capsule` と `style capsule` を support-only capsule として定義する
  - exact code owner / exact file mapping / implementation prompt / actual archive を未固定のまま stop boundary を書く
- Exit:
  - package docs と state が same Stage B artifact roles と same stop boundary を共有している
- Close note:
  - Phase 04 は docs-only management slice であり implementation start ではない
  - completed Phase 05 closeout 後は default で further docs-only slice を作らず、next forward move は `stop_and_user_report` に戻す

### Phase 05 Surface Realization Card First Construction Slice

- Objective:
  - deepresearch closeout で keep した writer-facing `surface realization card` を `prompt_builder.py` の generation prompt handoff only に narrow に落とし、minimum role / field と rollback boundary を current package docs に固定する
- Hypothesis:
  - `surface realization card` を new fact source や opener owner ではなく writer-facing handoff block に限定すれば、`announcement / daily_story / branding` の fingerprint-side naturalness を broad redesign なしで upstream に寄せられる
- Owner:
  - `C:\tetie\notecode\plan\ui_source_blog_contract_redesign_2026-04-18\`
  - `C:\tetie\notecode\docs\current_codex_state.md`
  - `C:\tetie\notecode\note\simple_note_pipeline\prompt_builder.py`
- Tasks:
  - exact code owner を `prompt_builder.py` 1 file に narrow に固定する
  - exact file mapping を generation prompt の writer-facing `[SURFACE_REALIZATION_CARD]` handoff block に narrow に固定する
  - minimum fields を `paragraph_breath / sentence_length_band / ending_mix / nominalization_budget / subject_visibility / connective_tolerance` に固定する
  - rollback boundary を `prompt_builder.py` 単独 diff に閉じる
- Exit:
  - current docs と current implementation が same owner / same file mapping / same minimum field read を共有している
- Close note:
  - current slice は `prompt_builder.py` の writer-facing handoff only であり、opener owner / fact owner / repair owner を増やさない
  - owner-local verification は `C:\tetie\notecode\note\tests\test_simple_note_pipeline.py` 1-file test-lane で `announcement / daily_story / branding` の 3 case を lock し、`[SURFACE_REALIZATION_CARD]` block と field 6 本の分化まで pass とする
- current next legal move が `pipeline.py` / quality guard / repair / routing / broader eval lane / threshold canonicalization / package-wide owner table / implementation prompt / actual archive を要するため、phase closeout 後は `stop_and_user_report`

### Phase 06 Surface Realization Card Offline Eval-First Slice

- Objective:
  - completed Phase 04 / completed Phase 05 baseline を崩さず、current mainline の offline eval artifact から next owner を narrow に判定する
- Hypothesis:
  - `run_current_mainline_genre_sweep.py --phase genre-rerun --genres announcement,daily_story,branding` の `live=False` limited eval なら、`SURFACE_REALIZATION_CARD` handoff が visible surface separation に寄与しているか、次に触る owner が writer-side 継続か downstream かを production runtime edit なしで切り分けられる
- Owner:
  - `C:\tetie\notecode\plan\ui_source_blog_contract_redesign_2026-04-18\`
  - `C:\tetie\notecode\docs\current_codex_state.md`
  - `C:\tetie\notecode\tools\run_current_mainline_genre_sweep.py` invocation only
  - `C:\tetie\notecode\logs\current_mainline_ui_runs\phase06_surface_realization_card_eval\`
- Tasks:
  - `run_current_mainline_genre_sweep.py --phase genre-rerun --genres announcement,daily_story,branding` を `live=False` で実行する
  - `summary.json / matrix.json / genre_issue_ledger.json / manual_review.md / research_notes.md` を self-test artifact として確認する
  - eval result が single-owner narrow continuation に落ちるなら docs sync 後に next slice を 1 本だけ開く
  - eval result が `pipeline.py` / quality guard / repair / routing / threshold canonicalization / multi-owner planning / actual archive を直ちに要するなら停止する
- Exit:
  - current eval artifacts が揃い、next move が still narrow か stop かを docs-only で説明できる
- Close note:
  - Phase 06 first slice は production runtime edit を含めない
  - each slice は self-test pass 後にだけ次へ進む
  - same slice error は 3 回まで自己修正し、未解決なら `stop_and_user_report`

## Refined Execution Order

- before any code diff:
  - `naturalness_recovery_2026-04-07` parked boundary と `opening_frame_redesign_2026-04-18` reference boundary を再確認する
- Phase 00:
  - package objective / non-goals / archive policy / follow-up prompt を fixed する
- Phase 01:
  - UI article contract と source role baseline を docs-only で固定する
- Phase 02:
  - Phase 01 baseline を崩さず、self-blog reuse policy / opener read boundary / zero-base artifact staging を docs-only で固定する
- Phase 03:
  - candidate inventory と current startup path docs の `do-not-archive` を確認し、actual archive に進まない
- only after docs phases pass:
  - post-docs management verdict を `hold_docs_only / ready_for_stage_a_scan / stop_and_user_report` の separate management step で判断する
- management step read:
  - first candidate は `Stage A telemetry only`
  - exact code owner / exact file mapping / implementation prompt / actual archive は fixed しない
- after current verdict:
  - Stage A telemetry-only scan が閉じた後、explicit user instruction があるときだけ Phase 04 `Stage B artifact boundary minimum` を docs-only で定義する
- completed Phase 05 closeout 後、explicit user instruction があるときだけ Phase 06 `surface realization card offline eval-first slice` を `run_current_mainline_genre_sweep.py` の `live=False` limited eval から開始する
- current Phase 06 では each slice を `self-test pass -> docs sync -> next slice` で進め、further progress が `pipeline.py` / quality guard / repair / routing / broader eval lane / threshold canonicalization / package-wide owner table / implementation prompt / actual archive` を要するなら `stop_and_user_report` を返す
- last resort:
  - stop and user report

## Retry Discipline

- 各 docs phase の自己修正は 3 回まで
- 3 回失敗したら new package docs と companion docs だけを rollback 対象として停止する
- failed framing は `PROGRESS.md` と `ROLLBACK.md` に明記する
