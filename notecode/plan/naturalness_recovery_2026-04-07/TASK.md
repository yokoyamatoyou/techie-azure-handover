# naturalness_recovery_2026-04-07 TASK

この package は blank `branding/company_introduction` の current judgment、non-company-introduction `branding` source contract keep、`comparative_review` source contract keep、`company_introduction_operational_source_contract_v1` runtime keep、company_introduction source-presentation / source-packet validation learning を docs lock / closeout まで固定する。  
`1 phase = 1 narrow hypothesis = 1 owner scope` を守り、archive-only records と completed / frozen reference package を reopen しない。

## Global Rules

- current success path を壊さない
- current objective は blank `branding/company_introduction` の residual、non-company-introduction `branding` source contract keep、`comparative_review` source contract keep、`company_introduction_operational_source_contract_v1` runtime keep、company_introduction source-presentation / source-packet validation learning の同期に閉じる
- pre-2026-04-02 records は archive-only に留める
- completed reference package を reopen しない
- frozen architecture package を reopen しない
- default route は `grounded generic` として扱う
- planning / skeleton route は feature gate を通った場合だけ opt-in する
- structural baseline は `single-pass + optional single repair 1回` として扱う
- `prompt_builder.py` simplification-first wording line は failed hypothesis / rollback 済みとして扱い、completed や current winner に書き換えない
- prompt-only は `floor` として keep するが、strongest baseline / best practice とは書かない
- skeleton / planning は `conditional signal only` として扱い、reopen candidate に戻さない
- public web article compare は evidence として保持し、current-business-first pattern を支持する補助根拠としてだけ使う
- direct GPT web compare は unavailable のままとし、WEB 勝利を主張しない
- technical explain / explanatory は current package に戻さない
- article type は fixed rule ではなく prior として使う
- article-type fixed routing table は追加しない
- prompt accretion 禁止
- hidden reviser accretion 禁止
- same failed hypothesis を unchanged で再投入しない
- `prompt_builder.py` と `newalgorithm_pipeline/pipeline.py` の同時 reopen はしない
- `newalgorithm_pipeline/pipeline.py` current-first source ordering / hint triage の unchanged retry はしない
- explicit user reopen / continue exception:
  - `simple_note_pipeline/pipeline.py` owner の narrow reopen / continue は許可する
- next owner は `simple_note_pipeline/pipeline.py` 1 file に閉じる
- hidden_late_validation_v1 は leakage / scope guard として keep する
- hidden_late_validation_v1 の active scope は `branding + semantic_article_key=company_introduction` のみに閉じる
- `branding_operational_source_contract_v1` は non-company-introduction branding の runtime source contract / craft guard として keep する
- `branding_operational_source_contract_v1` の active scope は `article_type=branding` かつ `semantic_article_key=""` / `branding` のみに閉じる
- `branding_operational_source_contract_v1` では `semantic_article_key=company_introduction` を明示的に除外する
- `comparative_review_source_contract_v1` は comparative_review の runtime source contract / 比較記事 craft guard として keep する
- `comparative_review_source_contract_v1` の active scope は `article_type=comparative_review` のみに閉じる
- `comparative_review_source_contract_v1` は persona / editor / trial names を runtime strategy にしない
- `comparative_review_source_contract_v1` は article-type fixed routing table を追加しない
- `comparative_review_source_contract_v1` は company_introduction を変更しない
- company_introduction source-presentation direct trial learning は `company_introduction_operational_source_contract_v1` の前段 evidence として扱う
- `company_introduction_operational_source_contract_v1` は company_introduction の runtime source contract / craft guard として keep する
- `company_introduction_operational_source_contract_v1` の active scope は `article_type=branding` かつ `semantic_article_key=company_introduction` のみに閉じる
- `company_intro_hidden_persona_contract_v1` の rollback / failed status を解除しない
- company_introduction runtime へ persona名を入れる前提にしない
- company_introduction source-packet direct/dry-run validation は runtime keep の前段 evidence として扱う
- company_introduction operational packet は persona 名ではなく source contract / craft / guard / validation trigger に圧縮した範囲だけ runtime keep とする
- company_introduction source contract required は `current_business` / `customer_situation_or_entry_point` / `support_scope_boundary` / `operating_process_steps` / `pre_contact_decision`
- company_introduction source contract optional は `proof_signal`
- company_introduction source contract guard-only は `source_limit`
- company_introduction source presentation では `company_posture` を required にしない
- company_introduction source presentation では `strengths` / `achievements` / `message` / `contact` を source-contract slot names として前面化しない
- company_introduction source contract guard は unsupported claim / source_limit visible leakage / brochure-only / generic-company-copy / abstract-philosophy-only / wrong article type drift に閉じる
- company_introduction retry trigger として ending monotony only / general naturalness / polish / length only / preference-only rewrite は使わない
- hidden checklist を増やさない
- acceptance 緩和は未実施のまま維持する
- next code hypothesis は未開始とし、次に触る場合も same owner `simple_note_pipeline/pipeline.py` で management 判断後に 1 本へ閉じる
- fail-closed keep diff を source-of-truth 同期前に捨てない
- `natural_blog_core.py` first section history clamp 仮説は do-not-retry
- `output_formatter.py` formatter-only surface polish 仮説は do-not-retry
- `input_contract.py` upstream distilled summary 単独仮説は do-not-retry
- `SECTION_SHADOW` reopen を初手に戻さない
- `quality_guard.py` first にしない
- repair acceptance reopen を初手にしない
  - ただし explicit user reopen exception として `simple_note_pipeline/pipeline.py` owner の narrow acceptance / scope gating reopen は許可する
- sentence-final monotony line を main candidate に戻さない
- next coding step は自動開始せず、regression / scope expansion / acceptance residual が明示された場合だけ management fixed prompt に従う
- 各 phase の自己修正は 3 回まで
- 3 回失敗したら rollback 後に停止し user report する

## Current Locked Outcome

- runtime keep-state:
  - default route は `grounded generic default`
  - planning / skeleton は `opt-in only`
  - structural baseline は `single-pass + optional single repair 1回`
- current keep diff:
  - latest `simple_note_pipeline/pipeline.py` reopen result は `V3 fail-closed keep`
  - history-first opener を success artifact として返さないときは `SYS_PIPELINE_FAILURE` に倒す
  - touched files は `simple_note_pipeline/pipeline.py` と `test_simple_note_pipeline.py`
  - rollback はなし
  - `V1 2/2 non-worse` / `V2 1/1 non-worse` / `G1 2/2 no visible regression`
- hidden_late_validation_v1 keep diff:
  - touched files は `simple_note_pipeline/pipeline.py` と `test_simple_note_pipeline.py`
  - `hidden_late_validation_v1` active scope は `branding + semantic_article_key=company_introduction` のみ
  - helper は `_build_hidden_late_validation_contract` / `_evaluate_hidden_late_validation` / `_hidden_late_validation_improved` / `_has_visible_forbidden_heading`
  - diagnostics は `hidden_late_validation` / `failure_candidate_summary`
  - first live validation は company intro `1/3 success`、leakage `0`、guard OK、hidden trigger `0`
  - fail payload live validation は company intro `2/3 success`、fail payload diagnostics preserved、leakage `0`、guard OK
  - fail run は hidden checklist 不足ではなく `company_intro_naturalness_not_improved` with `heading_reanchor` + `ending_bucket_monotony`
  - target recovery repeatability は未 close
  - rollback 不要
- branding_operational_source_contract_v1 keep diff:
  - touched files は `simple_note_pipeline/pipeline.py` / `simple_note_pipeline/prompt_builder.py` / `test_simple_note_pipeline.py`
  - active scope は `article_type=branding` かつ `semantic_article_key=""` / `branding`
  - `semantic_article_key=company_introduction` は除外
  - required source slots は `customer_touchpoint` / `operating_behavior` / `decision_principle` / `support_process` / `brand_posture_in_action`
  - optional source slot は `proof_signal`
  - retry allowed は missing required slot / unsupported claim / wrong article type drift / `philosophy_only` / `advertising_copy` / `abstract_value_only` / `customer_touchpoint_buried`
  - retry forbidden は `ending_bucket_monotony` only / general naturalness / polish / length only
  - live validation は full source `3/3 success`、sparse source `2/2 success`、unsupported claim guard success、non-target announcement guard success、visible leakage `0`、retry count `0`、pass gate `overall=true`
  - shared bundle residual `327 passed, 2 failed` は non-target residual として記録し、branding rollback reason にしない
  - rollback 不要
- comparative_review_source_contract_v1 keep diff:
  - touched files は `simple_note_pipeline/pipeline.py` / `simple_note_pipeline/prompt_builder.py` / `test_simple_note_pipeline.py`
  - active scope は `article_type=comparative_review`
  - comparative_review contract は company_introduction を定義しない
  - case_study / announcement / branding keep behavior は維持
  - required source slots は `comparison_context` / `evaluation_axes` / `option_differences` / `fit_conditions` / `tradeoffs_or_cautions` / `decision_next_step`
  - optional source slots は `price_or_plan` / `source_limit`
  - craft rule は勝敗・ランキングではなく条件別判断、同じ評価軸での複数候補比較、向く条件 / 避ける条件 / 確認順の維持、source-backed の価格 / プラン / 成果 / ベンダー優位だけに閉じる
  - retry allowed は missing required slot / unsupported price-plan-result-vendor claim / wrong article type drift / `ranking_drift` / `absolute_winner_drift` / `exaggerated_superiority` / fit_conditions missing / tradeoff_or_caution missing
  - retry forbidden は `ending_bucket_monotony` only / general naturalness / polish / length only / preference-only rewrite
  - live validation は comparative full source `3/3 success`、comparative sparse/source-limited `2/2 success`、unsupported price/plan/result/vendor guard success、non-target announcement guard success、visible leakage `0`、retry count `2 optional repair invocations`、disallowed retry ids `0`、pass gate `overall=true`
  - shared bundle residual `327 passed, 2 failed` は `st05ab10 comparative_axis_lock = SYS_LLM_CLIENT_REQUIRED` と `st08b4 industry_analysis title/lead residual` として記録し、comparative_review_source_contract_v1 rollback reason にしない
  - rollback 不要
- company_introduction_operational_source_contract_v1 keep diff:
  - touched files は `simple_note_pipeline/pipeline.py` / `simple_note_pipeline/prompt_builder.py` / `test_simple_note_pipeline.py`
  - active scope は `article_type=branding` かつ `semantic_article_key=company_introduction`
  - case_study / announcement / non-company-introduction branding / comparative_review keep behavior は維持
  - required source slots は `current_business` / `customer_situation_or_entry_point` / `support_scope_boundary` / `operating_process_steps` / `pre_contact_decision`
  - optional source slot は `proof_signal`
  - guard-only source slot は `source_limit`
  - craft rule は会社紹介を顧客接点 / 支援範囲 / 進め方 / 相談前判断へ戻し、brochure-only / generic-company-copy / abstract-philosophy-only drift を避ける
  - persona / editor / trial names は runtime prompt に入れない
  - `source_limit` は本文に出さず unsupported claim guard としてだけ使う
  - retry allowed は missing required slot / unsupported claim / wrong article type drift / brochure-only / generic-company-copy / abstract-philosophy-only / source_limit visible leakage
  - retry forbidden は ending monotony only / general naturalness / polish / length only / preference-only rewrite
  - live validation は full operational source `9/9`、thin process source `6/6`、company-profile negative `3/3 expected reject`、unsupported induced guard `5/5 detected / rejected`、non-target live `4/4 inactive / pass`
  - persona/editor/trial leakage `0`、source_limit visible leakage `0`、active live retry_count `0`、retry boundary `6/6`、disallowed retry ids `[]`、errors `[]`、residual `None`、pass gate `overall=true`
  - rollback 不要
- company_introduction source-presentation direct trial learning:
  - これは `company_introduction_operational_source_contract_v1` runtime keep 前の historical learning として保持する
  - `company_intro_hidden_persona_contract_v1` の過去 rollback は維持する
  - fixed condition は best persona / editor / regeneration 条件を固定し、source presentation 4種 x 3 runs で比較した
  - `operational packet` が best current candidate
  - `operational packet` は brochure `0` / generic `0` / abstract close `0` / unsupported `0` / leakage `0` / naturalness `strong 3/3`
  - required candidate は `current_business` / `customer_situation_or_entry_point` / `support_scope_boundary` / `operating_process_steps` / `pre_contact_decision`
  - optional candidate は `proof_signal`
  - guard-only candidate は `source_limit`
  - not required candidate は `company_posture`
  - avoid source-contract slot names は `strengths` / `achievements` / `message` / `contact`
  - `source_limit` は visible article ではなく unsupported claim guard としてだけ使う候補
  - follow-up source-packet direct/dry-run validation は下記 learning として記録済み
- company_introduction source-packet dry-run validation learning:
  - これは `company_introduction_operational_source_contract_v1` runtime keep 前の historical validation learning として保持する
  - validation 時点では runtime pipeline は実行せず、source packet preflight + direct/dry-run validation として実施した
  - scenario / run count は full operational source `3 sources x 3 runs = 9`、thin process source `2 sources x 3 runs = 6`、company-profile negative `3 cases`、unsupported claim guard `5 induced cases`、non-target guard `4 article types`、total `27 validations`
  - preflight result は full operational source `pass`、thin process source `warn-boundary`、company-profile negative `fail`、unsupported guard `pass + guard-risk`、non-target `inactive`
  - final validation は full operational source `pass`、thin process source `warn-pass`、company-profile negative `expected reject`、unsupported guard `guard pass`、non-target guard `pass`
  - full operational source は required 5 slots reflected / unsupported `0` / leakage `0` / brochure-generic-abstract `0`
  - thin process source は process thin だが `support_scope_boundary` / `pre_contact_decision` で厚みを補完し、source外の手順追加はなかった
  - company-profile negative は operational coverage fail と brochure / generic drift `3/3` を検出した
  - unsupported guard は `5/5` detected / rejected、source_limit visible leakage `0`
  - non-target guard は announcement / branding / case_study / comparative_review で inactive
  - operational packet preflight は後続の live validation で runtime keep に昇格した
  - historical learning 単独では `company_intro_hidden_persona_contract_v1` の rollback / failed status を解除しない
- floor:
  - `prompt-only`
- conditional signal only:
  - `skeleton / planning`
- failed hypothesis:
  - `C:\tetie\notecode\note\simple_note_pipeline\prompt_builder.py`
  - `heading_drift_reconstruction_simplification_first`
  - rollback 済み
  - kept diff なし
  - unchanged retry 禁止
  - V1 partial / non-worse
  - G1 no visible regression
  - V2 still awkward variance
  - V3 mandatory gate fail
  - V3 run2 は title history-first
  - V3 run3 は first section history-first
  - `C:\tetie\notecode\note\newalgorithm_pipeline\pipeline.py`
  - `pipeline_current_first_triage`
  - rollback 済み
  - kept diff なし
  - unchanged retry 禁止
  - V3 company intro guard 3/3 fail
- next owner:
  - `C:\tetie\notecode\note\simple_note_pipeline\pipeline.py`
- next narrow hypothesis:
  - next code hypothesis はまだ開始しない
  - `company_introduction_operational_source_contract_v1` は runtime keep のため、次に触るなら regression / scope expansion / acceptance residual が明示された場合だけ same owner の新 hypothesis として扱う
  - `heading_reanchor` + `ending_bucket_monotony` の acceptance boundary だけを自然さ一般の retry trigger にしない
  - `ending_monotony` を緩めすぎて flat artifact を success に戻さない
- do-not-retry:
  - `prompt_builder.py` simplification-first wording line の unchanged retry
  - `pipeline.py` current-first source ordering / hint triage の unchanged retry
  - `natural_blog_core.py` first section history clamp
  - `output_formatter.py` formatter-only surface polish
  - `input_contract.py` upstream distilled summary only
- evidence boundary:
  - deepresearch は evidence のみ
  - public web article compare と direct GPT web compare は分けて扱う
- package close state:
  - docs closeout for `company_introduction_operational_source_contract_v1`
  - next implementation window は不要

## Gates

### Entry Gate

- current baseline / rollback boundary / active reference boundary が `README.md` / `PROGRESS.md` / `ROLLBACK.md` に固定されている
- owner scope が 1 file に閉じている
- phase hypothesis が upstream root と downstream containment を同時に混ぜていない

### Pass Gate

- owner-local tests pass
- shared checks pass
- current success path regression なし
- rollback note 更新済み
- visible symptom について `why this owner` を telemetry と artifact で説明できる
- `C:\tetie\notecode\docs\stepwise_three_article_gate_2026-04-08.md` の固定 3 cases で `generic` と `step-optimized` の live compare を実施済み
- target 2 cases で `step-optimized` が `generic` より AI 感が少なく、blog 一貫性を保つ
- guard 1 case で visible regression がない

### Planning Opt-In Gate

- `ordering_signal` が立っている
- `no source, no standalone section` を満たせる
- `grounded_sections` と `grounded_section_ratio` が source-backed ordering benefit を支える
- `article type` は fixed rule ではなく prior としてだけ扱う
- `distinct_fact_clusters >= 3` の単独 hard gate ではなく、`dense_grounding_composite` を含む複合 gate として説明できる
- must-cover を section role 分離で扱う意味がある
- narrative compression が低い
- visible template cost より ordering benefit が大きい
- `company / announcement / daily` は current priors 上 refusal 寄りを維持する
- この gate を artifact と telemetry で説明できるまでは、default route を `grounded generic` から変更しない
- Phase 03 closeout 後は、technical explain の残課題をこの gate の追加緩和ではなく coverage-first source readiness の別問題として扱い、current package から split out of package する

### Stop Gate

- same owner hypothesis で 3 回失敗
- current keep diff を守ったまま success artifact を返せない
- current success path regression
- rollback 不可能な diff が必要
- owner scope を超えないと前進できない
- multiple owner simultaneous edit でしか前進できない

## Shared Checks

### Newalgorithm / Mainline Boundary

```text
C:\tetie\notecode\.venv\Scripts\python.exe -m pytest note\tests\test_newalgorithm_phase03_pipeline.py note\tests\test_current_mainline_runner.py note\tests\test_current_mainline_regressions.py -q
```

### Focused Simple Note / Quality

```text
C:\tetie\notecode\.venv\Scripts\python.exe -m pytest note\tests\test_simple_note_pipeline.py note\tests\test_simple_note_quality_guard.py -q
```

### UI Matrix Boundary

```text
C:\tetie\notecode\.venv\Scripts\python.exe -m pytest note\tests\test_current_mainline_ui_matrix.py -q
```

## Phase Map

### Phase 00 Route / Style / Repair Telemetry Freeze

- Objective:
  - route ownership / style ownership / repair refusal を separate window で再監査できる telemetry に固定する
- Hypothesis:
  - `writer_of_record`、`style_profile_source`、`patch_path_refusal_reason` が見えれば、upstream root と downstream containment を混同せずに narrow fix を進められる
- Owner:
  - `C:\tetie\notecode\note\newalgorithm_pipeline\pipeline.py`
- Tasks:
  - writer / route / style source / planned vs actual heading overlap の telemetry を追加する
  - repair refusal / patch refusal の理由コードを結果に残す
- Exit:
  - latest artifact で route / style / repair refusal がログだけで説明できる

### Phase 01 Repair Trigger For Ending Monotony

- Objective:
  - ending monotony を detect-only から repair-triggered へ寄せる
- Hypothesis:
  - `ending_bucket_max_run >= 3` で repair trigger を boost すれば、文末単調の repair が空振りしにくくなる
- Owner:
  - `C:\tetie\notecode\note\simple_note_pipeline\quality_guard.py`
- Tasks:
  - repair trigger に ending monotony boost を追加する
  - repair trigger 内訳 telemetry を出す
- Exit:
  - ending monotony が検出された run で repair_required 判定の理由を説明できる

### Phase 02 Repair Patch Path And Acceptance For Branding

- Objective:
  - branding/company introduction の ending repair を concrete patch path と acceptance に接続する
- Hypothesis:
  - branding を patch path に含め、acceptance に ending monotony 改善条件を追加すれば、repair が本文改善へ繋がる
- Owner:
  - `C:\tetie\notecode\note\simple_note_pipeline\pipeline.py`
- Tasks:
  - `_compact_plan_safe_scope()` へ branding を追加する
  - repair acceptance に ending monotony 改善条件を追加する
- Exit:
  - patch_path_used / ending monotony diff が latest-style artifact で説明できる
  - known blockage を owner-local に外したと言える
  - broad flatness が残っても次 phase の owner を narrow に説明できる

### Phase 03 Planning Opt-In Feature Gate

- Objective:
  - `grounded generic default` を維持したまま、planning route を narrow feature gate 経由の opt-in に閉じる
- Hypothesis:
  - `source density / must-cover density / narrative compression / ordering benefit` を gate 化すれば、planning を default にせず必要なケースだけへ限定できる
- Owner:
  - `C:\tetie\notecode\note\newalgorithm_pipeline\pipeline.py`
- Entry condition:
  - docs-first route reframe 完了後である
  - local evidence が `grounded generic default + planning opt-in` を current interpretation として支持している
- Tasks:
  - planning opt-in の feature gate を追加する
  - article-type priors は prior としてだけ扱い、fixed routing table を増やさない
  - `no source, no standalone section` を route entry 条件に含める
- Exit:
  - generic default が non-opt-in case で保たれる
  - opt-in case だけで ordering benefit が generic を上回る、または gate failure reason を narrow に説明できる
- Close note:
  - `2026-04-12 21:54 JST` の validation で `company / announcement / daily` は refusal のまま generic default を維持し、`ui-short-case-study-explain` は `dense_grounding_composite` で opt-in した
  - `bl-explanatory-misread-metric` は `grounded_sections_insufficient` で refusal のままで、Phase 03 の strict hard gate 問題とは切り分けて扱う

### Phase 04 Company Intro Source-Aware Plan Prune

- Objective:
  - unsupported section を generic filler で埋める pressure を減らす
- Hypothesis:
  - source buckets に応じて `強み / 歩み / 提供価値` default slots を prune すれば、意味の薄い説明句が減る
- Owner:
  - `C:\tetie\notecode\note\natural_blog_core.py`
- Tasks:
  - company introduction default slots を source-aware にする
  - unsupported planned section telemetry を出す
- Exit:
  - source-backed section ratio が説明でき、unsupported slots が残らない

### Phase 05 Prompt Surface Constraint Preservation

- Objective:
  - simple prompt 的な micro-surface 指示を current mainline で落とさない
- Hypothesis:
  - `prompt_surface_items` を contract に保持すれば、prompt accretion なしで writer に自然さ制御を伝えられる
- Owner:
  - `C:\tetie\notecode\note\newalgorithm_pipeline\input_contract.py`
- Tasks:
  - raw / kept / dropped surface items を保持する
  - writer が消費した surface items を telemetry に残す
- Exit:
  - dropped surface items と drop reason が latest artifact で見える

## Refined Execution Order

- before next diff:
  - latest failed hypothesis / next owner / stop boundary を docs-first で固定する
- current docs lock で固定すること:
  - `prompt_builder.py` simplification-first wording line は failed hypothesis / rollback 済みと書く
  - unchanged retry しないと書く
  - prompt-only は floor、skeleton / planning は conditional signal only として書く
  - public web article compare は current-business-first pattern 支持として残す
  - direct GPT web compare unavailable を残す
- not current package next diff:
  - technical explain / explanatory article の coverage-first source readiness 診断
  - article-type fixed routing table の追加
  - prompt-only winner / best practice の断定
  - skeleton / planning の default reopen
- after docs lock:
  - management window が `simple_note_pipeline/pipeline.py` owner の narrow acceptance boundary を見るか、current keep diff のまま park / observe するかを判断する
- code diff only if management re-evaluate says continue:
  - owner と narrow hypothesis が新しく 1 file に閉じた場合だけ再開する
- last resort:
  - owner-local stop and user report

理由:
- blank company intro の latest failed hypothesis を source-of-truth に固定しないまま code lane を増やすと、historical keep diff と rolled-back attempt が再混線する
- prompt-only は勝ち筋の floor だが strongest baseline ではなく、skeleton / planning も winner ではない
- `technical explain` は current package objective 外であり、same window に戻すと評価軸が崩れる
- `prompt_builder.py` wording simplification 単独は V3 mandatory gate を越えられなかったため、same owner unchanged retry より upstream owner へ narrow に切り替えるほうが安全
- したがって current package の次の 1 step は docs lock 後の management 判断で止め、code change は `simple_note_pipeline/pipeline.py` owner の narrow hypothesis が新しく固定された場合だけ再開する

### Phase 06 Branding Normalize Relaxation

- Objective:
  - branding/company introduction の改行呼吸を normalize で均しすぎない
- Hypothesis:
  - branding だけ `_normalize_output_lines()` の強さを弱めれば、 paragraph breath が少し戻る
- Owner:
  - `C:\tetie\notecode\note\newalgorithm_pipeline\output_formatter.py`
- Entry condition:
  - upstream root correction と repair containment 後も paragraph breath が不自然に残る場合だけ開始する
- Exit:
  - normalize 前後の paragraph metrics で改善が説明できる
- Close note:
  - formatter-only surface polish は current hypothesis では do-not-retry とする

## Retry Discipline

- 同一 phase で 3 回失敗したら rollback して停止する
- failed hypothesis は `PROGRESS.md` と `ROLLBACK.md` に明記する
- prompt-only strengthening を comparative gate なしで retry option に入れない

## Slim Audit Freeze 2026-04-28

- status:
  - `SLIM_AUDIT_READY_SINGLE_OWNER`
  - `NO_IMMEDIATE_DELETION_SAFE`
- rule:
  - completion candidate の本文 mainline を壊さない
  - この audit から product code / prompt / persona / threshold / repair / guard / UI / image / pipeline 変更へ直行しない
  - live rerun / full-flow rerun はこの docs-only owner では行わない
- next implementation:
  - current window: `no implementation owner`
  - 次 window で slim 実装を開く場合の rank 1 candidate は `prompt_builder.py generic branding hint cleanup`
  - ただし deletion ではなく behavior-preserving cleanup とし、generic branding pass / explicit company-product-announcement-comparative guard を落とさない検証設計を先に固定する
- do not touch from this audit:
  - company_introduction 成功を支える prompt / source contract / hidden late
  - fail-closed hard source-contract behavior
  - single repair acceptance
  - source fit pre-generation stop
  - GPT Image 2 / UI
  - product_introduction borrowed branding-family behavior
  - `simple_note_pipeline/pipeline.py` acceptance / hard-fail boundary

## Branding Anchor Live Validation Freeze 2026-04-28

- status:
  - `BRANDING_ANCHOR_LIVE_VALIDATION_PASS`
  - `BRANDING_ANCHOR_FIX_VALIDATED_NO_FURTHER_OWNER`
- artifact:
  - `C:\tetie\notecode\logs\branding_anchor_live_validation_20260428-233408`
- rule:
  - completion candidate の本文 mainline を維持する
  - complexity risk は維持する
  - freeze list は継続する
  - この validation record から product code / prompt / persona / threshold / repair / guard / UI / image / pipeline 変更へ直行しない
  - 追加生成しない
  - 追加改善しない
  - owner を作らない
- validation result:
  - anchorあり generic branding: `3/3 pass`
  - anchorなし generic branding: `2/2 expected block`
  - missing-anchor block reason: `INP_SOURCE_CONTEXT_INSUFFICIENT`
  - missing-anchor body: `0`
  - missing-anchor LLM calls: `0`
  - guard pass: company_introduction / product_introduction / announcement / case_study / comparative_review
  - product code changed: `NO`
  - product code hash diff: `NO_PRODUCT_CODE_HASH_DIFF`
- next implementation:
  - current window: `no implementation owner`
  - 次に実装する場合は、新しい artifact-backed blocker が出た時のみ `1 issue = 1 owner`
